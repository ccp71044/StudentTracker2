<UserControl x:Class="StudentTracker.Wpf.Views.CertificatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="CertificatesView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Certificate Orders" AutomationProperties.AutomationId="CertificatesHeader" Style="{StaticResource ViewHeaderStyle}" Grid.Row="0" />
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            <Button Content="New Order" Style="{StaticResource ActionButtonStyle}" Command="{Binding NewOrderCommand}" />
            <Button Content="Record Delivery" Style="{StaticResource ActionButtonStyle}" Command="{Binding RecordDeliveryCommand}" />
            <Button Content="Refresh" Style="{StaticResource ActionButtonStyle}" Command="{Binding RefreshCommand}" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder}" AutoGenerateColumns="False" Grid.Row="2" Margin="10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding DisplayId}" Width="100" />
                <DataGridTextColumn Header="Ordered" Binding="{Binding OrderedDate, StringFormat=dd/MM/yyyy}" Width="120" />
                <DataGridTextColumn Header="Status" Binding="{Binding OrderStatus}" Width="120" />
                <DataGridTextColumn Header="Count" Binding="{Binding ItemCount}" Width="80" />
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=C}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
