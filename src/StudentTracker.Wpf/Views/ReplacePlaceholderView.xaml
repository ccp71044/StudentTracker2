<UserControl x:Class="StudentTracker.Wpf.Views.ReplacePlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="500" MinHeight="280">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Style="{StaticResource ViewHeaderStyle}" Grid.Row="0" />
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Allocation:" VerticalAlignment="Center" Margin="0,4" Grid.Row="0" Grid.Column="0" />
            <TextBlock Text="{Binding AllocationDisplayId}" VerticalAlignment="Center" Margin="8,4" Grid.Row="0" Grid.Column="1" />

            <TextBlock Text="Placeholder:" VerticalAlignment="Center" Margin="0,4" Grid.Row="1" Grid.Column="0" />
            <TextBlock Text="{Binding PlaceholderName}" VerticalAlignment="Center" Margin="8,4" Grid.Row="1" Grid.Column="1" />

            <TextBlock Text="Replace with student:" VerticalAlignment="Center" Margin="0,4" Grid.Row="2" Grid.Column="0" />
            <ComboBox ItemsSource="{Binding AvailableStudents}" SelectedItem="{Binding SelectedStudent}"
                      DisplayMemberPath="FullName" Grid.Row="2" Grid.Column="1" Margin="8,4" />
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,10,0,0">
            <Button Content="Save" Style="{StaticResource ActionButtonStyle}" Command="{Binding SaveCommand}" />
            <Button Content="Cancel" Style="{StaticResource ActionButtonStyle}" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
