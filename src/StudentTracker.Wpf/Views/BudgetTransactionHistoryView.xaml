<UserControl x:Class="StudentTracker.Wpf.Views.BudgetTransactionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="650" MinHeight="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" Style="{StaticResource ViewHeaderStyle}" Grid.Row="0" />
        <DataGrid ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" IsReadOnly="True" Grid.Row="1" Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Reference" Binding="{Binding DisplayId}" Width="120" />
                <DataGridTextColumn Header="Date" Binding="{Binding TransactionDate, StringFormat={}{0:g}}" Width="140" />
                <DataGridTextColumn Header="Type" Binding="{Binding TransactionType}" Width="180" />
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="130" />
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="250" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,10,0,0">
            <Button Content="Close" Style="{StaticResource ActionButtonStyle}" Command="{Binding CloseCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
