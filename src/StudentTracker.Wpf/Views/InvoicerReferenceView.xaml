<UserControl x:Class="StudentTracker.Wpf.Views.InvoicerReferenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             AutomationProperties.AutomationId="InvoicerReferenceView">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Invoicer References" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" AutomationProperties.AutomationId="InvoicerReferenceHeader" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Import CSV" Command="{Binding ImportCommand}" Padding="8,4" Margin="0,0,6,0" AutomationProperties.AutomationId="InvoicerReferenceImportButton" />
            <Button Content="Export Client Prepaid Position" Command="{Binding ExportClientPrepaidPositionCommand}" Padding="8,4" Margin="0,0,6,0" AutomationProperties.AutomationId="InvoicerReferenceExportButton" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="8,4" AutomationProperties.AutomationId="InvoicerReferenceRefreshButton" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Invoices}" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False" SelectedItem="{Binding SelectedInvoice}" AutomationProperties.AutomationId="InvoicerReferenceDataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice Number" Binding="{Binding InvoiceNumber}" Width="*" />
                <DataGridTextColumn Header="Customer" Binding="{Binding Customer}" Width="*" />
                <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate, StringFormat=d}" Width="Auto" />
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=C}" Width="Auto" />
                <DataGridTextColumn Header="Assigned" Binding="{Binding AmountAssignedToStudentTracker, StringFormat=C}" Width="Auto" />
                <DataGridTextColumn Header="Status" Binding="{Binding PaymentStatus}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Foreground="DimGray" Margin="0,8,0,0" TextWrapping="Wrap" AutomationProperties.AutomationId="InvoicerReferenceStatusText" />
    </Grid>
</UserControl>
