<UserControl x:Class="StudentTracker.Wpf.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="StudentsView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Students" AutomationProperties.AutomationId="StudentsHeader" Style="{StaticResource ViewHeaderStyle}" Grid.Row="0" />
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            <TextBox Text="{Binding SearchText}" Width="200" Margin="0,0,10,0" VerticalContentAlignment="Center" AutomationProperties.AutomationId="StudentsSearchTextBox" />
            <Button Content="Search" Style="{StaticResource ActionButtonStyle}" Command="{Binding SearchCommand}" AutomationProperties.AutomationId="StudentsSearchButton" />
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0" />
            <Button Content="Add Student" Style="{StaticResource ActionButtonStyle}" Command="{Binding AddStudentCommand}" AutomationProperties.AutomationId="AddStudentButton" />
            <Button Content="View" Style="{StaticResource ActionButtonStyle}" Command="{Binding ViewStudentCommand}" ToolTip="Open read-only student profile." AutomationProperties.AutomationId="ViewStudentButton" />
            <Button Content="Edit" Style="{StaticResource ActionButtonStyle}" Command="{Binding EditStudentCommand}" AutomationProperties.AutomationId="EditStudentButton" />
            <Button Content="Add Allocation" Style="{StaticResource ActionButtonStyle}" Command="{Binding AddAllocationCommand}" ToolTip="Create a new allocation for the selected student." AutomationProperties.AutomationId="StudentsAddAllocationButton" />
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0" />
            <Button Content="Refresh" Style="{StaticResource ActionButtonStyle}" Command="{Binding RefreshCommand}" AutomationProperties.AutomationId="RefreshStudentsButton" />
            <CheckBox Content="Show archived" IsChecked="{Binding ShowArchived}" VerticalAlignment="Center" Margin="10,0" AutomationProperties.AutomationId="StudentsShowArchivedCheckBox" />
            <CheckBox Content="Edit Table" IsChecked="{Binding IsInlineEditingEnabled}" VerticalAlignment="Center" Margin="10,0" AutomationProperties.AutomationId="StudentsEditTableCheckBox" />
        </StackPanel>
        <DataGrid x:Name="StudentsDataGrid" ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}" AutoGenerateColumns="False" Grid.Row="2" Margin="10"
                 IsReadOnly="{Binding IsInlineEditingEnabled, Converter={StaticResource InvertedBooleanConverter}}"
                 CanUserAddRows="False" CanUserDeleteRows="False"
                 RowEditEnding="DataGrid_RowEditEnding"
                 MouseDoubleClick="DataGrid_MouseDoubleClick"
                 PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="View" Command="{Binding ViewStudentCommand}" AutomationProperties.AutomationId="StudentsContextMenu_View" />
                    <MenuItem Header="Edit" Command="{Binding EditStudentCommand}" AutomationProperties.AutomationId="StudentsContextMenu_Edit" />
                    <MenuItem Header="Add Allocation" Command="{Binding AddAllocationCommand}" AutomationProperties.AutomationId="StudentsContextMenu_AddAllocation" />
                    <Separator />
                    <MenuItem Header="Archive" Command="{Binding DeleteStudentCommand}" AutomationProperties.AutomationId="StudentsContextMenu_Archive" />
                    <MenuItem Header="Restore" Command="{Binding RestoreStudentCommand}" AutomationProperties.AutomationId="StudentsContextMenu_Restore" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student ID" Binding="{Binding DisplayId}" Width="100" IsReadOnly="True" />
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120" />
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" IsReadOnly="True" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180" />
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120" />
                <DataGridTextColumn Header="Employer" Binding="{Binding Employer}" Width="120" />
                <DataGridTextColumn Header="Work Group" Binding="{Binding WorkGroup}" Width="120" />
                <DataGridTextColumn Header="Manager" Binding="{Binding Manager}" Width="120" />
                <DataGridTextColumn Header="Group Tag" Binding="{Binding GroupTag}" Width="120" />
                <DataGridCheckBoxColumn Header="Duplicate" Binding="{Binding PotentialDuplicate}" Width="80" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
