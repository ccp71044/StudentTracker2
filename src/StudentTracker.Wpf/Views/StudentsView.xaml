<UserControl x:Class="StudentTracker.Wpf.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="StudentsView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Students" AutomationProperties.AutomationId="StudentsHeader" Style="{StaticResource ViewHeaderStyle}" Grid.Row="0" />
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            <TextBox Text="{Binding SearchText}" Width="200" Margin="0,0,10,0" VerticalContentAlignment="Center" />
            <Button Content="Search" Style="{StaticResource ActionButtonStyle}" Command="{Binding SearchCommand}" />
            <Button Content="Add Student" Style="{StaticResource ActionButtonStyle}" Command="{Binding AddStudentCommand}" />
            <Button Content="View" Style="{StaticResource ActionButtonStyle}" Command="{Binding ViewStudentCommand}" ToolTip="Open read-only student profile." />
            <Button Content="Edit" Style="{StaticResource ActionButtonStyle}" Command="{Binding EditStudentCommand}" />
            <Button Content="Archive" Style="{StaticResource ActionButtonStyle}" Command="{Binding DeleteStudentCommand}" ToolTip="Mark the selected student as archived." />
            <Button Content="Restore" Style="{StaticResource ActionButtonStyle}" Command="{Binding RestoreStudentCommand}" />
            <CheckBox Content="Show archived" IsChecked="{Binding ShowArchived}" VerticalAlignment="Center" Margin="10,0" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}" AutoGenerateColumns="False" Grid.Row="2" Margin="10" IsReadOnly="True" 
                 MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student ID" Binding="{Binding DisplayId}" Width="100" />
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120" />
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180" />
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120" />
                <DataGridTextColumn Header="Employer" Binding="{Binding Employer}" Width="120" />
                <DataGridCheckBoxColumn Header="Duplicate" Binding="{Binding PotentialDuplicate}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
