<UserControl x:Class="StudentTracker.Wpf.Views.CoursesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="CoursesView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Course Definitions" AutomationProperties.AutomationId="CoursesHeader" Style="{StaticResource ViewHeaderStyle}" Grid.Row="0" />
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            <TextBox Text="{Binding SearchText}" Width="200" Margin="0,0,10,0" VerticalContentAlignment="Center" AutomationProperties.AutomationId="CoursesSearchTextBox" />
            <Button Content="Search" Style="{StaticResource ActionButtonStyle}" Command="{Binding SearchCommand}" AutomationProperties.AutomationId="CoursesSearchButton" />
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0" />
            <Button Content="Add Course" Style="{StaticResource ActionButtonStyle}" Command="{Binding AddCourseCommand}" AutomationProperties.AutomationId="AddCourseButton" />
            <Button Content="Edit" Style="{StaticResource ActionButtonStyle}" Command="{Binding EditCourseCommand}" AutomationProperties.AutomationId="EditCourseButton" />
            <Button Content="Add Delivery" Style="{StaticResource ActionButtonStyle}" Command="{Binding AddDeliveryCommand}" ToolTip="Create a new delivery for the selected course." AutomationProperties.AutomationId="CoursesAddDeliveryButton" />
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0" />
            <Button Content="Refresh" Style="{StaticResource ActionButtonStyle}" Command="{Binding RefreshCommand}" AutomationProperties.AutomationId="RefreshCoursesButton" />
            <CheckBox Content="Show inactive" IsChecked="{Binding ShowInactive}" VerticalAlignment="Center" Margin="10,0" AutomationProperties.AutomationId="CoursesShowInactiveCheckBox" />
            <CheckBox Content="Edit Table" IsChecked="{Binding IsInlineEditingEnabled}" VerticalAlignment="Center" Margin="10,0" AutomationProperties.AutomationId="CoursesEditTableCheckBox" />
        </StackPanel>
        <DataGrid x:Name="CoursesDataGrid" ItemsSource="{Binding Courses}" SelectedItem="{Binding SelectedCourse}" AutoGenerateColumns="False" Grid.Row="2" Margin="10"
                 IsReadOnly="{Binding IsInlineEditingEnabled, Converter={StaticResource InvertedBooleanConverter}}"
                 CanUserAddRows="False" CanUserDeleteRows="False"
                 RowEditEnding="DataGrid_RowEditEnding"
                 MouseDoubleClick="DataGrid_MouseDoubleClick"
                 PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Edit" Command="{Binding EditCourseCommand}" AutomationProperties.AutomationId="CoursesContextMenu_Edit" />
                    <MenuItem Header="Add Delivery" Command="{Binding AddDeliveryCommand}" AutomationProperties.AutomationId="CoursesContextMenu_AddDelivery" />
                    <Separator />
                    <MenuItem Header="Archive" Command="{Binding DeleteCourseCommand}" AutomationProperties.AutomationId="CoursesContextMenu_Archive" />
                    <MenuItem Header="Restore" Command="{Binding RestoreCourseCommand}" AutomationProperties.AutomationId="CoursesContextMenu_Restore" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding CourseCode}" Width="120" IsReadOnly="True" />
                <DataGridTextColumn Header="Title" Binding="{Binding CourseTitle}" Width="300" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" />
                <DataGridTextColumn Header="Duration (days)" Binding="{Binding CourseDurationDays}" Width="110" />
                <DataGridTextColumn Header="Provider" Binding="{Binding Provider}" Width="150" />
                <DataGridTextColumn Header="Default Cost" Binding="{Binding DefaultCertificateCost}" Width="110" />
                <DataGridTextColumn Header="Default Credit" Binding="{Binding DefaultCreditQuantity}" Width="110" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="250" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
