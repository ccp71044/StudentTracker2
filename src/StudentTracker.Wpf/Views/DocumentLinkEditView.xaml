<UserControl x:Class="StudentTracker.Wpf.Views.DocumentLinkEditView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" MinWidth="520">
    <Grid Margin="20">
        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Link Document" Style="{StaticResource ViewHeaderStyle}"/>
        <TextBlock Grid.Row="1" Text="Record type:" Margin="0,6"/><ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding EntityTypes}" SelectedItem="{Binding SelectedEntityType}" Margin="8,4"/>
        <TextBlock Grid.Row="2" Text="Record:" Margin="0,6"/><ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Targets}" SelectedItem="{Binding SelectedTarget}" DisplayMemberPath="FriendlyName" Margin="8,4"/>
        <TextBlock Grid.Row="3" Text="Purpose:" Margin="0,6"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Purpose}" Margin="8,4"/>
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0"><Button Content="Link" Command="{Binding SaveCommand}" Style="{StaticResource ActionButtonStyle}"/><Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource ActionButtonStyle}"/></StackPanel>
    </Grid>
</UserControl>
