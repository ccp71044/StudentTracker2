<UserControl x:Class="StudentTracker.Wpf.Views.DeliveriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="DeliveriesView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Course Deliveries" AutomationProperties.AutomationId="DeliveriesHeader" Style="{StaticResource ViewHeaderStyle}" Grid.Row="0" />
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="1">
            <Button Content="Add Delivery" Style="{StaticResource ActionButtonStyle}" Command="{Binding AddDeliveryCommand}" />
            <Button Content="Edit" Style="{StaticResource ActionButtonStyle}" Command="{Binding EditDeliveryCommand}" />
            <Button Content="View Allocations" Style="{StaticResource ActionButtonStyle}" Command="{Binding ViewAllocationsCommand}" ToolTip="View allocations for the selected delivery." />
            <Button Content="Add Allocation" Style="{StaticResource ActionButtonStyle}" Command="{Binding AddAllocationCommand}" ToolTip="Create a new allocation for the selected delivery." />
            <Button Content="Cancel Delivery" Style="{StaticResource ActionButtonStyle}" Command="{Binding CancelDeliveryCommand}" />
            <Button Content="Refresh" Style="{StaticResource ActionButtonStyle}" Command="{Binding RefreshCommand}" />
            <CheckBox Content="Edit Table" IsChecked="{Binding IsInlineEditingEnabled}" VerticalAlignment="Center" Margin="10,0" />
        </StackPanel>
        <DataGrid x:Name="DeliveriesDataGrid" ItemsSource="{Binding Deliveries}" SelectedItem="{Binding SelectedDelivery}" AutoGenerateColumns="False" Grid.Row="2" Margin="10"
                 IsReadOnly="{Binding IsInlineEditingEnabled, Converter={StaticResource InvertedBooleanConverter}}"
                 CanUserAddRows="False" CanUserDeleteRows="False"
                 RowEditEnding="DataGrid_RowEditEnding"
                 MouseDoubleClick="DataGrid_MouseDoubleClick"
                 PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Edit" Command="{Binding EditDeliveryCommand}" />
                    <MenuItem Header="View Allocations" Command="{Binding ViewAllocationsCommand}" />
                    <MenuItem Header="Add Allocation" Command="{Binding AddAllocationCommand}" />
                    <Separator />
                    <MenuItem Header="Cancel Delivery" Command="{Binding CancelDeliveryCommand}" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Delivery ID" Binding="{Binding DisplayId}" Width="100" IsReadOnly="True" />
                <DataGridTextColumn Header="Course Code" Binding="{Binding CourseDefinition.CourseCode}" Width="120" IsReadOnly="True" />
                <DataGridTextColumn Header="Status" Binding="{Binding DeliveryStatus}" Width="100" IsReadOnly="True" />
                <DataGridTextColumn Header="Date Status" Binding="{Binding DateStatus}" Width="100" IsReadOnly="True" />
                <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}" Width="80" />
                <DataGridTextColumn Header="Start" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="120" />
                <DataGridTextColumn Header="End" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="120" />
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="150" />
                <DataGridTextColumn Header="Trainer" Binding="{Binding TrainerName}" Width="150" />
                <DataGridTextColumn Header="Business Details" Binding="{Binding TrainerBusinessDetails}" Width="200" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
